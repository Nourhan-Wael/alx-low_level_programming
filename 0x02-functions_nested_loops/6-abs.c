#include "main.h"

/**
 * _abs - check the sign of the number
 * @n: is a parameter
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
if (n < 0)
{
n = -1 * n;
return (n);
}
else
{
return (n);
}

}





