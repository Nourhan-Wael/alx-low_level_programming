#include<stdio.h>
/**
 * main - Entry point
 * @argc: a parameter
 * @argv: a parameter
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}

