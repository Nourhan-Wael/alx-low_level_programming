#include "main.h"

/**
 * _isalpha - check all the alphabet in small
 * @c: is a parameter
 * Return: Always 0 (Success)
 */

int _isalpha(int c)
{
if (c >= 65 && c <= 122)
return (1);
else
return (0);
}





