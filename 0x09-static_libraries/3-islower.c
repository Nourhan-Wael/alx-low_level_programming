#include "main.h"

/**
 * _islower - check all the alphabet in small
 * @c: is a parameter
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
if (c >= 97 && c <= 122)
return (1);
else
return (0);
}





